server:
  port: 8081

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/plazo
    username: root
    password:

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
appDescription: "Reto Powerup"
appVersion: "1.0.0"

